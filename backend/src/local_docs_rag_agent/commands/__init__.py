"""CLI command handlers.

Each handler takes an `AppConfig` plus already-parsed arguments and prints its result,
so the argument parsing in `cli.py` stays separable from what a command actually does.
"""
